<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>

        <item name="colorPrimaryDark">@color/color_primary_dark</item>

        <item name="colorAccent">@color/color_accent_night</item>

        <item name="colorAccentedSurface">@color/color_divider_light</item>

        <item name="colorIcon">@android:color/white</item>

        <item name="colorDivider">@color/color_divider_dark</item>

        <item name="android:statusBarColor">@android:color/black</item>
        <item name="toolbarColor">@color/toolbarColor_night</item>
        <item name="bgColor">@color/bg_color_night</item>
        <item name="archiveBgColor">@color/color_gray_dark</item>
        <item name="titleColor">@android:color/white</item>
        <item name="messageColor">@color/color_gray</item>
        <item name="counterColor">@color/color_gray_dark</item>
        <item name="dataColor">@color/color_gray</item>
        <item name="swipeColor">@color/swipe_color_night</item>
        <item name="indicatorColor">@color/swipe_color_night</item>
    </style>
</resources>